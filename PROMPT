❯ Ok - I was working on one project idea - I had called it prpolish and it is located over here:
  ../../moovfinancial/prpolish

  However, I think I was going down a bad path.  I may reference some thigns from that project, but really we are
  going to start from scratch here.

  The main idea is this:
  1) I want to build a command line tool that builds new AI powered command line tools.
  2) It would all start from a config file.  The config would define certain characteristics of the tool.
     The config might include prompts (or references to prompt files) that would all be used
     to define the behavior of the new cli tool.
  3) The tool we are building here in this directory will be called tool-builder (at least for now).
     In the end tool-builder should be a command line tool anyone could download as a single app.
     However, it might reference other libraries, prompts, etc from the main github repo that could be used
     to build the new tool.

There is a lot more to discuss on the design.

But first let's put some basics in place:
* Plese do a git init so we start saving all we do in git.
* Add a directory called PLANS.  This should be used to store all memories you need
  on why and how we are building this project.  I want those plans stored in this directory so
  can keep them in version control.  You should keep updating those plans as we discuss more changes
  concerning the tool-builder.
